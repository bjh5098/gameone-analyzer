# CLAUDE.md

이 폴더는 gameone.kr 박스스코어를 상황별(아웃카운트·주자상황 등) 조건으로 필터링해
타자/투수 스탯을 뽑는 개인 분석 프로젝트다.

## 배경

- 대상 클럽: 한양대학교 D-Dogs OB (`club_idx=31993`)
- "우리팀" 정의는 [`games.md`](./games.md) 상단 참조
- 팀 채팅에서 나온 요구사항을 기반으로 필터 조건을 정의함 (2026-08-10, 이기준·배준혁 논의)
- Why: 엑셀 필터처럼 조건을 조합해 "이 상황에서 이 선수/팀 스탯이 어땠는지"를 즉시 조회하고 싶어함.
  세부 UI보다 먼저 정확한 필터 조건 정의가 우선이라는 합의가 있었음.

## 페이지 구조 (확정)

**타자 관점 페이지**, **투수 관점 페이지** 2개로 나뉜다. 필터 조건 구조는 두 페이지가
동일하고, 집계되는 스탯 종류만 다르다(타자: 타율/안타/타점 등, 투수: 피안타율/자책점 등).

## 필터 feature 정의 (확정, 두 페이지 공통)

아래 축을 조합해 필터링 가능해야 한다:

- **아웃카운트**: 무사 / 1사 / 2사
- **타순** (1~9번, 개별 다중선택): 이기준 요청(2026-08-15) — "1·2번/3·4·5번/6~9번" 3그룹
  묶음이 아니라 개별 타순 체크박스로 구현, 여러 개 선택해 조합 가능
- **주자상황** (8가지, 상호 배타): 주자없음, 1루, 2루, 3루, 1·2루, 1·3루, 2·3루, 만루
  - 득점권 = 2루 또는 3루 점유(2루/3루/1·2루/1·3루/2·3루/만루)
- **구장 그룹핑**: 단일 구장 조회와 여러 구장을 합산한 조회를 모두 지원 (예: 살곶이만 / 살곶이+배재고)
- **연도**: 시즌(2024/2025/2026) 단독 및 다중 시즌 합산 조회
- **리그**: 리그명(일요 싱글, 일요메이져, 일요리그(C조), 수요 야간 등) 단독 및 다중 리그 합산 조회
  - 구장과 리그는 서로 독립된 축 — 동일 리그가 여러 구장에서 열리거나, 동일 구장에서 여러 리그가 진행될 수 있음

## 확장 feature (가능하면, 우선순위 낮음)

- **점수차**: 1점차/2점차/3점차/4점차 이상
  - [확인 필요] 타석 시점 기준 점수차 정의(동점 처리 방식 등)가 아직 미확정. 구현 전 사용자에게 재확인.

## 득점권/상황 판별 방법 (파싱·시뮬레이션 로직)

gameone.kr은 타석 단위로 아웃카운트·주자상황을 직접 제공하지 않음. 타자기록 테이블에서
`행 = 타순(1~9번), 열 = 이닝(1~12), 셀 = 그 이닝 그 타순 선수의 결과 이벤트 코드`
형태로만 제공되므로, 이닝별 타자 순서(타순 순환)를 따라가며 셀의 이벤트 코드를
순서대로 적용해 매 타석 시작 시점의 (아웃카운트, 주자상황) 상태를 역산 시뮬레이션한다.

의사코드 (이전 세션에서 검증한 로직):
```
state = {1B: None, 2B: None, 3B: None, outs: 0}
for inning in 1..N:
    order = 이전 이닝 마지막 타순 다음부터 이어짐 (9번 다음은 1번으로 순환)
    state = {1B: None, 2B: None, 3B: None, outs: 0}  # 이닝 시작 시 리셋
    for batter in order (그 이닝에 셀이 채워진 타순만, 순서대로):
        is_risp = (state.2B is not None) or (state.3B is not None)  # 득점권 여부
        record(batter, inning, state.outs, state.runner_pattern, is_risp)
        events = parse(cell_text)  # 콤마로 분리, 예: ["4구","도루","도루"]
        apply_events_to_state(events, state)  # 진루/아웃/득점 처리
```

**검증 방법(필수)**: 시뮬레이션 결과의 이닝별 총득점을 실제 스코어보드 표(팀별 이닝 점수)와
대조하고, 선수별 득점 합계 컬럼과도 대조해서 일치하는지 확인한다. 불일치 시 이벤트 코드
해석 로직을 재검토.

### 확인된 이벤트 코드 예시 (사전 구축 필요)

지금까지 예시 경기 1건에서 관찰된 코드. 실제 파서 구현 전 더 많은 경기로 코드 사전을 확장해야 함.

| 코드 | 의미 |
|---|---|
| `4구` | 볼넷 |
| `사구` | 몸에 맞는 볼 (HBP) — README/CLAUDE 필터에는 아직 명시 안 됐지만 볼넷과 유사 처리 |
| `삼진` | 삼진 아웃 (1아웃) |
| `우안`, `좌안`, `중안` | 우전/좌전/중전 안타 (1루타) |
| `좌전2`, `우중2`, `중월홈` 등 숫자/홈 | 2루타, 3루타(3 표기), 홈런(홈 표기) — 타구 방향+진루루 조합 |
| `유땅병살`, `병살` | 병살타 (2아웃) |
| `유플`, `중플`, `투플` 등 | 내야/외야 플라이 아웃 (1아웃) |
| `중희플`, `희타`, `희비` | 희생플라이/희생타 (1아웃, 주자 진루) |
| `투야선`, `실책`, `3실` | 야수선택/실책 (아웃 없이 진루, 숫자는 실책한 포지션 번호로 추정) |
| `도루` | 도루 성공 (주자 진루, 아웃 없음) |
| `주자아웃`, `견제사` | 주자 아웃 (다른 주자의 진루 실패, 아웃 카운트 증가) |
| `폭투`, `보크` | 주자 진루 (아웃 없음) |
| `대주자` | 선수 교체(대주자 투입) — 타석 결과 아님, 별도 처리 필요 |
| `1직` | 투수 직선타 아웃 (1아웃) |

- 콤마로 묶인 여러 이벤트가 한 셀에 있을 때, 이후 이벤트가 "그 타자 본인"의 후속
  주루인지 "다른 주자"의 동시 이벤트인지 코드만으로는 모호한 경우가 있음(1회
  이기준 타석 사례 참고) → 스코어보드 대조로 보정하는 로직이 필요.

## 데이터 소스 (현재 저장된 파일)

- 전체 대상 경기 URL 목록: [`games.md`](./games.md) (2024~2026시즌, 95경기, 몰수/기권 경기 제외)
- 경기별 메타데이터(연도/날짜/리그/구장/상대팀/스코어): [`games_meta.csv`](./games_meta.csv)
  - `club/info/schedule/result?season={year}&club_idx=31993&...&page={n}` 목록 페이지에서 추출
  - **구장명 표기 불일치 주의**: `구의 야구장`/`구의야구장`, `살곶이 야구장`/`살곶이야구장` 등 공백 유무가
    섞여 있음. 구장 그룹핑 필터 구현 시 정규화(공백 제거 등) 필요.
  - 이 CSV에 있는 리그 목록: 일요 싱글, 일요메이져, 일요리그(C조), 수요 야간, 일요 3부, 원외리그,
    서울시민리그(S-리그), 생활체육서울시민리그, 디비전 6-강남구, 성동구청장기 야구대회,
    친선경기, 그 외 대회성 리그 소수
- 95경기 원문 HTML: `data/raw/{game_idx}.html` (로컬 캐시, `.gitignore`에 포함되어 git에는 없음 —
  재수집하려면 `scripts/fetch_all.py` 실행)
- 파싱·시뮬레이션 결과: `data/gameone.db` (SQLite, `.gitignore`에 포함 — `scripts/build_db.py`로 재생성)
- 정적 사이트용 JSON: [`docs/data_batter.json`](./docs/data_batter.json),
  [`docs/data_pitcher.json`](./docs/data_pitcher.json),
  [`docs/data_roster.json`](./docs/data_roster.json) (git에 커밋됨, `scripts/build_site_data.py`로 재생성)

## 완료된 작업

1. ~~`games_meta.csv` + `games.md`를 기반으로 실제 파서 스크립트 작성~~ — 완료
   (`src/gameone_analyzer/{fetch,parser,events,simulator,validator,stats,export}.py`)
2. ~~이벤트 코드 사전을 경기 몇 건 더 훑어보며 보강~~ — 완료 (95경기 전체 스캔, UNKNOWN 코드 0개)
3. ~~파싱 결과를 경기별로 캐싱해서 저장할 형식 결정~~ — 완료 (`data/raw/{game_idx}.html` + `data/gameone.db`)
4. 타자 관점/투수 관점 2개 페이지 — 완료. Vanilla HTML/JS 정적 사이트로 GitHub Pages 배포
   (https://bjh5098.github.io/gameone-analyzer/)
5. 배포 후 사용자(이기준) 피드백으로 추가된 기능:
   - 스탯 테이블 컬럼 정렬(오름차순/내림차순 토글) — `docs/js/table-sort.js`
   - OPS/피OPS, 삼진율(K%) 컬럼 + 최소 PA/BF 입력 필터 — `docs/js/{batter,pitcher}-page.js`
   - 리그 필터 체크박스 라벨을 "요일+구장별칭+부수" 조합으로 표시, 대회성 리그는
     "(대회)"로 별도 분류, 시즌별로 이름이 바뀐 동일 리그는 체크박스 병합 —
     `docs/js/league-label.js` (아래 "리그별 구장/부수 매핑" 섹션 참고)
   - 야구 스탯 사이트 느낌의 UI 리디자인 — `docs/css/style.css`
   - 시즌 필터를 폼 최상단으로 이동
6. gameone.kr 공식 랭킹 페이지(`/club/info/ranking/hitter`, `/club/info/ranking/pitcher`,
   club_idx=31993) 컬럼을 참고해 추가 스탯 반영 (컬럼 순서도 랭킹 페이지 순서에 맞춤):
   - 타자: 1루타, 루타(TB), 희타(SAC), 희비(SF), 고의4구(IBB), 병살(GIDP), 장타율(SLG),
     BB/K, 장타/안타(XBH/H) — `src/gameone_analyzer/export.py`의
     `is_intentional_walk`/`is_gidp`, `docs/js/batter-page.js`
   - 투수: 희타, 희비, 고의4구, 폭투(WP), 보크(BK) — `export.py`의
     `has_wild_pitch`/`has_balk`, `docs/js/pitcher-page.js`
   - **의도적으로 제외한 항목**: 득점(R)/타점(RBI)/도루(SB)/방어율(ERA)/이닝(IP)/WHIP/
     승패. 이유: (1) 득점/타점/도루는 "어떤 주자가 득점했는지"를 코드만으로 특정 선수에게
     귀속시킬 수 없음 (2) 방어율/이닝/WHIP/승패는 경기 전체 단위 기록이라 아웃카운트·
     주자상황 등 타석 단위 부분 필터링과 개념적으로 안 맞음(필터링된 부분 타석만으로는
     "이닝"이 성립하지 않음). 득점 시뮬레이션 정확도를 실제로 검증해본 결과 팀 합계
     기준 스코어보드 대비 약 21% 과소집계(569 vs 721, 95경기 전체 합산)로 나타나
     선수 귀속은 물론 팀 합계로도 신뢰할 수 없다고 판단.
7. 타순 필터 추가 (2026-08-15, 이기준 팀채팅 요청) — 완료. `parser.py`/`simulator.py`/
   `build_db.py`(`plate_appearances.batting_order`)에 타순이 이미 저장되고 있었고
   `export.py`의 JSON 직렬화 단계에만 누락돼 있어, **raw HTML 재수집 없이** 기존
   `data/gameone.db` 캐시로 `export.py`에 `batting_order` 필드 추가 후
   `scripts/build_site_data.py` 재실행만으로 반영. `docs/js/filters.js`(`battingOrders`
   축 추가) + `docs/batter.html`/`pitcher.html`에 1~9번 체크박스(다중선택, OR 조건).
8. "출장 현황" 탭 추가 (2026-09-01, 팀 채팅에서 송우석 요청 — 풀오버 무료 배포 대상자를
   올해 참가 경기수 기준으로 뽑기 위함) — 완료. `docs/roster.html` +
   `docs/js/roster-page.js`, `scripts/build_site_data.py`의 `_build_roster_records`
   (신규 `docs/data_roster.json`).
   - **집계 기준**: `plate_appearances`(타석 시뮬레이션 결과) 테이블이 아니라 원본
     타자기록/투수기록 표를 직접 읽음(`export.py`의 `collect_appearance_names`,
     `parse_batter_rows`/`parse_pitcher_rows` 재사용) — 대수비만 들어가고 타석에
     한 번도 안 선 선수는 `plate_appearances`에 안 잡히지만 타자기록표에는 행이
     남아있어서 이 방식으로만 잡힌다.
   - 투수/타자 동시 출전 시 게임 단위로 dedup(한 경기 = 1경기, 역할 무관 합산 아님) —
     타순 필터 때처럼 단순 합계로 처리하면 중복 집계된다는 사용자 피드백 반영.
   - **의도적으로 범위에서 뺀 것**: 게임원 기록지에 안 잡히는 오프라인 출석/노쇼/지각,
     개막식·MT 등 행사 참여도. 이유: 기록지 파싱 기반 도구라 "경기에 못 나온 오프라인
     참석"은 원천적으로 데이터가 없음(사용자도 대화 중 확인) — 내년 이후 위치기반
     자체 출결 웹앱으로 별도 구축 예정이라고 함. 이 도구는 "게임원 기록지 기준 출장
     경기수"까지만 우선 제공.

## 리그별 구장/부수 매핑 (사용자 확인, `docs/js/league-label.js`에 구현)

리그 필터 체크박스에 "어느 구장/몇 부인지"를 표시하기 위해 사용자에게 직접 확인한 매핑.
새로운 리그가 데이터에 추가되면 여기 표를 먼저 갱신하고 `league-label.js`의
`LEAGUE_TAGS`/`CONFIRMED_CHECKBOX_LABELS`/`VENUE_ONLY_CHECKBOX_LABELS`/`LEAGUE_GROUPS`에
반영한다 — 부수를 추측해서 넣지 않는다(오표기 위험이 추측 이득보다 큼).

| 원본 리그명 (games_meta.csv) | 실제 구장 | 구장별칭 | 부수 | 체크박스 표시 |
|---|---|---|---|---|
| 일요 싱글 | 살곶이야구장 | 성동 | 3부 | 일요성동3부 |
| 수요 야간 | 살곶이야구장 | 성동 | 4부 | 수요성동4부 |
| 일요메이져 | 배재고야구장 | 배재고 | 3부 | 일요배재고3부 |
| 일요 3부 | 배명중,고 야구장 | 배명 | 3부 | 일요배명3부 |
| 일요리그(C조) | [서울] 성남고/성남중 (2개 구장) | 성남 | 4부 | 일요성남4부 |
| 원외리그 | 살곶이야구장 | 성동 | (미확인) | 성동원외리그 |
| 생활체육서울시민리그 (2024) | 난지/신월 | — | (미확인) | "서울시민리그"로 병합 표시 |
| 서울시민리그(S-리그) (2025) | 난지/신월 | — | (미확인) | 위와 동일 리그, 같은 체크박스 |
| 디비전 6-강남구 | 구의/난지 | — | (미확인) | 원래 이름 유지 |
| 성동구청장기 야구대회 등 대회성 리그 | 다양 | — | 대회(부수 대상 아님) | "OOO (대회)" |

- "생활체육서울시민리그"와 "서울시민리그(S-리그)"는 시즌에 따라 이름이 바뀐 동일
  리그임을 사용자가 확인 — `LEAGUE_GROUPS`에서 체크박스 하나로 병합, 필터링 시 두
  원본 리그명 모두를 포함.
- 일요리그(C조)는 성남고/성남중 두 구장에 걸쳐 있어 특정 구장명 대신 지역 별칭
  "성남"을 씀(살곶이야구장 → "성동"과 동일한 방식).

## 남은 작업

- 점수차 필터([확인 필요] 상태)를 사용자와 재확인 후 구현
- 콤마/슬래시 이벤트 체인 파싱으로도 완전히 해소되지 않는 소수 이닝의 스코어보드 불일치
  (야수 에러/야수선택 뒤 정확한 진루 베이스 수가 코드만으로 확정 불가) — README.md "알려진 한계" 참고
- 위 매핑 표에서 "(미확인)"으로 남은 리그(원외리그, 서울시민리그, 디비전 6-강남구)의
  부수를 사용자에게 재확인 후 반영

## 작업 시 주의사항

- robots.txt상 ClaudeBot 등 자동화 봇이 gameone.kr 전체에서 차단되어 있음. 개인/팀 데이터 소규모 조회
  목적으로만 최소 요청 진행 중이며, 대량/반복 크롤링 코드를 작성하기 전에는 사용자와 반드시 재확인할 것.
  요청 간 지연을 두고 서버 부담을 최소화할 것.
- gameone.kr에는 JSON API가 없음(서버사이드 렌더링 HTML만 존재, Playwright로 네트워크 캡처해 확인함).
  HTML 직접 파싱이 유일한 경로.
- 로그인 없이 박스스코어 데이터 열람 가능(확인됨). 로그인 필요 없음.
